package com.example.balvinder.newsapplication.exceptions;

/**
 * Created by balvinder on 18/12/17.
 */

public enum ErrorMessage {
    REJECTED, CONNECTION, GSON, TIMEOUT, NETWORK_NOT_AVAILABLE,
    PULL_TOKEN_ERROR, TIME_OUT_EXCEPTION, OTHER_ERROR, NO_ERROR,
    SYNC_TOKEN_ERROR, NOT_FOUND, DUPLICATE_APPOINTMENT;
}
